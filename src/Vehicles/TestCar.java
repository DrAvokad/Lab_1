package Vehicles;

import Machine.Engine;
import Abstracts.Movable;

import java.awt.*;

public class TestCar {

/*    Engine testEngine = new Engine(100);



    //----------Methods----------

    private double speedFactor = testEngine.getEnginePower() * 0.01;

    public TestCar(double x, double y, double size, Direction direction, Color color) {
        super(x, y, size, direction, color, "TestCar");
    }

    public void startEngine(double currentSpeed){
        setCurrentSpeed(testEngine.startEngine());
    }

    public boolean getEngineOn(){
        return testEngine.getEngineOn();
    }

    public void gas(double amount){
        setCurrentSpeed(testEngine.gas(amount, getCurrentSpeed(), speedFactor));
    }

    public void brake(double amount){
        setCurrentSpeed(testEngine.brake(amount,getCurrentSpeed(),speedFactor));
    }

    @Override
    double speedFactor() {
        return 0;
    }*/
}
