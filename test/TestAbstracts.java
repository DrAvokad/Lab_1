import Abstracts.Movable;
import Abstracts.Positionable;
import org.junit.Assert;
import org.junit.Test;

import java.awt.*;

import static org.junit.Assert.*;

public class TestAbstracts {

    //----------Positionable----------

    /*
    @Test
    public void testPositionableConstructor(){
        Positionable pos = new Positionable(0, 0, 100);
        assertTrue(pos.getSize()==100);
    }
    */

    //----------Movable----------


}
